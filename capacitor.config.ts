import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ProyectoFacheritoXD',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
